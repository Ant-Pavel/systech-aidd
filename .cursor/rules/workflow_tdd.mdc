---
alwaysApply: true
---
# Workflow разработки (TDD)

## 🎯 Основной принцип
Строго следовать плану из `docs/tasklist.md`. Итерации выполняются последовательно, без пропусков. Разработка ведется по TDD подходу.

## 📋 Алгоритм работы

### 1. Планирование итерации
- Изучить текущую итерацию в `docs/tasklist.md`
- Предложить решение с конкретными отрезками кода
- **Ожидать согласования** перед реализацией

### 2. TDD Реализация

#### 🔴 RED - Написать падающий тест
1. Создать тест для новой функциональности
2. Тест должен быть **специфичным** и проверять одно поведение
3. Убедиться что тест **падает** (запустить `make test`)
4. **Ожидать подтверждения** перед переходом к GREEN

#### 🟢 GREEN - Минимальный код для прохождения
1. Написать **минимальный** код для прохождения теста
2. Следовать принципам из `docs/conventions.md` и `docs/vision.md`
3. Придерживаться KISS - никакого оверинжиниринга
4. Убедиться что все тесты **зеленые** (`make test`)
5. **Ожидать подтверждения** перед переходом к REFACTOR

#### 🔵 REFACTOR - Улучшить код
1. Улучшить код без изменения поведения
2. Убрать дублирование (DRY)
3. Улучшить читаемость и структуру
4. Убедиться что тесты остаются **зелеными**
5. Проверить coverage (`make test-cov` ≥ 70%)

**Цикл:** RED → GREEN → REFACTOR → (повторить при необходимости)

### 3. Подтверждение
- После реализации **ожидать подтверждения** от пользователя
- Проверить работоспособность при необходимости
- Убедиться что `make quality` проходит успешно

### 4. Обновление прогресса
- Отметить выполненные задачи в `docs/tasklist.md` (чекбоксы [x])
- Обновить статус итерации (⏳ → 🚧 → ✅)
- Указать дату завершения итерации

### 5. Коммит
- Сделать коммит с описанием на английском
- Формат: `<тип>: <описание>`
- Примеры: `feat: add echo mode`, `fix: fix error handling`

### 6. Следующая итерация
- **Согласовать переход** к следующей итерации
- Не начинать новую итерацию без подтверждения

## ⚠️ Важно
- ❌ Не отклоняться от плана
- ❌ Не пропускать согласование
- ❌ Не переходить к следующей задаче без подтверждения
- ❌ Не писать код без теста (TDD!)
- ❌ Не пропускать этапы RED-GREEN-REFACTOR
- ✅ Обновлять tasklist после каждой задачи
- ✅ Делать атомарные коммиты
- ✅ Ждать подтверждения на каждом этапе
- ✅ Всегда начинать с падающего теста
- ✅ Проверять coverage (≥ 70%)
